# Game Rules for Send Gold mod
# Allows players to choose if gold can be sent to AI or only players

send_gold_ai_rule = {
	name = send_gold_ai_rule
	desc = send_gold_ai_rule_desc
	default = players_only
	
	option = {
		name = players_only
		desc = send_gold_players_only_desc
	}
	
	option = {
		name = allow_ai
		desc = send_gold_allow_ai_desc
	}
}
